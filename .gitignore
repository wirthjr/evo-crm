.env
.env.production
.env.local
.env.*.local
**/.env
**/.env.production
**/.env.local
**/.env.*.local
_evo*

.claude
.claude/
.claude/*
.agents/*
CLAUDE.md

# Sibling repos cloned locally but not registered in .gitmodules. They were
# accidentally tracked as orphan gitlinks before; ignore the dirs so nobody
# re-adds them with `git add`.
evolution-hub
evo-ai-docs/
evo-flow/
evo-crm-enterprise/
